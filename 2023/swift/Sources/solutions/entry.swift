import types

class Solution {
    let day: Int
    let folder: String
    let verbose: Bool

    init(day: Int, folder: String, logger: Logger) {
        self.day = day
        self.folder = folder
        self.verbose = false
    }

    func run() {

    }
}
